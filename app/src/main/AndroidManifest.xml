<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aona2.studywithme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyWithMe">
        <activity android:name=".NewView.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewView.UserSettingActivity">

        </activity>
        <activity android:name=".OldView.LoginActivity" />
        <activity android:name=".OldView.StudyActivity" />
        <activity
            android:name=".OldView.TaskNameInputActivity"
            android:noHistory="true" />
        <activity android:name=".OldView.HomeActivity">

        </activity>
        <activity android:name=".OldView.RegisterActivity">

        </activity>
    </application>

</manifest>